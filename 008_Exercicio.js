//Exercício💗

const usuario = "gabrilla_richardes";
const usuarioDigitado = "gabrilla_richardes";
const senha = 2912;
const senhaDigitada = 291;
if (usuario == usuarioDigitado && senha == senhaDigitada) {
    console.log('Usuário logado');

} else if (senha != senhaDigitada) {
    console.log('Senha inválida');

} else {
    console.log('Usuário inválido');
}